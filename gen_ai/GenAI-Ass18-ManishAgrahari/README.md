# Assignment 18 - Text Vectorization Techniques

This assignment covers common NLP vectorization techniques used to convert text into numerical features.

The assignment uses the cleaned and lemmatized text stored in the `final_processed_text` column.

## Requirements

Install the required libraries:

```bash
pip install pandas numpy scikit-learn

## How to Run
## Open the Jupyter Notebook.
## Create/load the dataset.
## Run the previous NLP preprocessing steps up to lemmatization.
## Make sure the following column exists:
## df["final_processed_text"]
## Run Tasks 1 to 10 sequentially.